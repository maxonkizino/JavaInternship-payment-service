apiVersion: v1
kind: Service
metadata:
  name: java-internship-payment-service
  labels:
    app: java-internship-payment-service
spec:
  selector:
    app: java-internship-payment-service
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
  type: ClusterIP
  